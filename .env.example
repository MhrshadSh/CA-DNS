# Copy to .env (or run `make env`, which also generates secrets).
# .env is gitignored; never commit real values.

# --- PostgreSQL ---------------------------------------------------------------
POSTGRES_VERSION=18
POSTGRES_DB=cadns
POSTGRES_USER=cadns_owner
POSTGRES_PASSWORD=change-me
